extends layout.pug

block content
    h1 Search Results for "#{query}"
    if playlists.length > 0
        table
            each playlist in playlists
                tr
                    td= playlist.name
                    td: a(href=playlist.external_urls.spotify class='link') Link to Spotify Playlist
    else
        p No matching playlists found.
